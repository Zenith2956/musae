[phases.setup]
nixPkgs = ["php84", "php84Extensions.openssl", "php84Extensions.pdo", "php84Extensions.pdo_mysql", "php84Packages.composer", "nodejs_22"]
nixLibs = ["openssl"]

[phases.build]
cmds = [
  "composer install --no-dev --optimize-autoloader",
  "npm ci",
  "npm run build"
]

[phases.start]
cmd = "php artisan storage:link && php artisan serve --host 0.0.0.0 --port $PORT"
